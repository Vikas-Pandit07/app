# ==============================
# OUTLOOX Backend Configuration
# ==============================
# Import environment variables from .env when available.
# This is helpful for local development and ensures EMAIL_USERNAME/EMAIL_PASSWORD are loaded.
spring.config.import=optional:dotenv:
spring.application.name=outloox-backend
server.port=${SERVER_PORT:9090}
server.forward-headers-strategy=framework

# Database
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:outloox}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Flyway / JPA
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=false
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${JPA_FORMAT_SQL:false}
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.order_inserts=true

# JWT
jwt.secret=${JWT_SECRET:change-me-to-a-long-random-secret-key-at-least-32-characters}
jwt.expiration=${JWT_EXPIRATION:3600000}

# Email
spring.mail.host=${EMAIL_HOST:smtp.gmail.com}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.username=${EMAIL_USERNAME:ap691869@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:}
spring.mail.from=${EMAIL_FROM:${EMAIL_USERNAME:ap691869@gmail.com}}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Cloudinary
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}

# Razorpay
razorpay.key-id=${RAZORPAY_KEY_ID:}
razorpay.key-secret=${RAZORPAY_KEY_SECRET:}
razorpay.webhook-secret=${RAZORPAY_WEBHOOK_SECRET:}

# Frontend / cookies / CORS
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173}
app.cookie.secure=${APP_COOKIE_SECURE:false}
app.cookie.same-site=${APP_COOKIE_SAME_SITE:Lax}
app.cookie.domain=${APP_COOKIE_DOMAIN:}
app.seed.demo-catalog=${APP_SEED_DEMO_CATALOG:true}

# Rate limiting
security.rate-limit.enabled=${SECURITY_RATE_LIMIT_ENABLED:true}
security.rate-limit.window-seconds=${SECURITY_RATE_LIMIT_WINDOW_SECONDS:60}
security.rate-limit.auth-max-requests=${SECURITY_RATE_LIMIT_AUTH_MAX_REQUESTS:20}
security.rate-limit.payment-max-requests=${SECURITY_RATE_LIMIT_PAYMENT_MAX_REQUESTS:30}
# Only set to true once your reverse proxy strips any client-supplied X-Forwarded-For
# and sets its own trusted value. Leaving this false is safe but rate-limits by the
# proxy's own IP if you're behind one (fine for a single small deployment; revisit
# when you put nginx/a load balancer in front of the backend).
security.rate-limit.trust-proxy-headers=${SECURITY_RATE_LIMIT_TRUST_PROXY_HEADERS:false}

# File uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# Logging
logging.level.org.springframework.security=INFO
logging.level.com.outloox=INFO
